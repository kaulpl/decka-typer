# Release process

Stable plugin releases are created from `main` by GitHub Actions. The release workflow validates the plugin, builds the WordPress ZIP, creates the version tag and GitHub Release, and verifies release assets.
